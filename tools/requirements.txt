# Dépendances build-time du pipeline dataset (tools/build_dataset.py).
# Le runtime de l'app Android n'en dépend PAS : ces libs ne servent qu'à
# générer les assets committés. Installer avec :
#   pip install -r tools/requirements.txt
#
# pymupdf (fitz) : extraction des PDF fiches-essences (tools/essence_pdf.py).
pymupdf>=1.24,<2
# Pillow : recollage + encodage WebP des photos des fiches-essences
# (pymupdf ne sait pas encoder en WebP). Épinglé en major : un bump de
# libwebp peut re-churner tous les WebP committés (one-shot accepté).
pillow>=10,<12
